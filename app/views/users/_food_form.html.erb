
<%= form_tag user_path, method: :patch do %>
  <% @foods.each do |food| %>
    <%= check_box_tag :foods, food.id, @user.foods.include?(food), :name => 'foods[]' %>
    <%= label_tag :foods, food.name %>
  <% end %>
  <%= submit_tag "Submit", class: "btn btn-default center-block" %>
<% end %>
